import { CertificateTypePipe } from './certificate-type.pipe';

describe('CertificateTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CertificateTypePipe();
    expect(pipe).toBeTruthy();
  });
});
