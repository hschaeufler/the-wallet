import { TestManufacturerPipe } from './test-manufacturer.pipe';

describe('TestManufacturerPipe', () => {
  it('create an instance', () => {
    const pipe = new TestManufacturerPipe();
    expect(pipe).toBeTruthy();
  });
});
