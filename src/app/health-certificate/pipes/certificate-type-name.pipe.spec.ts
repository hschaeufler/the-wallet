import { CertificateTypeNamePipe } from './certificate-type-name.pipe';

describe('CertificateTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CertificateTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
