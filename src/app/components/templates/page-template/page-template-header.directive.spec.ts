import { PageTemplateHeaderDirective } from './page-template-header.directive';

describe('PageTemplateHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PageTemplateHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
