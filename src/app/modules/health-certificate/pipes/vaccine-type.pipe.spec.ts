import { VaccineTypePipe } from './vaccine-type.pipe';

describe('VaccineTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VaccineTypePipe();
    expect(pipe).toBeTruthy();
  });
});
