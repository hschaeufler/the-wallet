import { TargetDiseasePipe } from './target-disease.pipe';

describe('TargetDiseasePipe', () => {
  it('create an instance', () => {
    const pipe = new TargetDiseasePipe();
    expect(pipe).toBeTruthy();
  });
});
